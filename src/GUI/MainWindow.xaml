<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GUI"
        xmlns:oxy="http://oxyplot.org/wpf" x:Class="GUI.MainWindow"
        mc:Ignorable="d"
        Title="Math Interpreter" Height="450" Width="800">
    <Window.DataContext>
        <local:MainViewModel/>
    </Window.DataContext>
    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="_File">
                <MenuItem Header="Exit" x:Name="MenuExit" Command="{Binding ExitCommand}"/>
            </MenuItem>
            <MenuItem Header="_Help">
                <MenuItem Header="View Help"/>
                <MenuItem Header="About"/>
            </MenuItem>
        </Menu>
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="8*"/>
                <RowDefinition Height="3*"/>
                <RowDefinition Height="1*"/>
            </Grid.RowDefinitions>

            <oxy:PlotView Model="{Binding MyModel}"/>
            <TextBlock x:Name="AlertTextBlock" Grid.Row="1" TextAlignment="Center" Text="{Binding StatusMessage}"></TextBlock>
            <TextBox Grid.Row="2" MaxLength="48" />
        </Grid>
    </DockPanel>
</Window>
